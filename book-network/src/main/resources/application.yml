spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 20MB

springdoc:
  default-produces-media-type: application/json # swagger-ui custom path

server:
  servlet:
    context-path: /api/v1/
